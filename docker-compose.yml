version: '2'
services:
    web:
      image: tutum/lamp
      volumes:
        - .:/var/www/html
      ports:
          - "8080:80"
    spring:
      build: ./springylicious/bodymassindex
      ports:
          - "8081:8080"     
           

            
